<!DOCTYPE html>
<html>
  <head>
    <title>
      <% if content_for? :title %><%= yield :title %> - <% end %>Mystiform
    </title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=Edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= stylesheet_link_tag "application" %>
    <link href='https://fonts.googleapis.com/css?family=Work+Sans:400,500|Quicksand:700' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">

    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
    <!--[if lt IE 8]>
      <%= stylesheet_link_tag "bootstrap-ie7" %>
    <![endif]-->
    <%= csrf_meta_tag %>
  </head>
  <body data-controller="<%= controller.controller_path %>"
        data-action="<%= controller.action_name %>">

    <div class="wrapper">
      <%= render "layouts/header" unless @disable_header %>

      <% if flash.present? %>
        <div class="container">
          <div class="row">
            <div class="col-md-12">
              <% flash.each do |key, value| %>
                <%= content_tag :div, value, class: flash_class(key) if value.present? %>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>

      <div class="container">
        <%= yield %>
      </div>

      <%= render "layouts/footer" %>
    </div>

    <%= yield :javascript %>

    <%= javascript_include_tag :application %>
  </body>
</html>
